<!DOCTYPE html>
<html>

<head>
    <title>Giấy Gửi Hàng</title>
    <meta charset="UTF-8">
    <style>
        /* Reset và thiết lập cơ bản */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: Arial, sans-serif;
            font-size: 10px;
            width: 210mm;
            height: 297mm;
            margin: 0;
            padding: 5mm;
        }

        /* Container chính sử dụng grid */
        .container {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            grid-template-rows: repeat(3, 1fr);
            gap: 5mm;
            width: 100%;
            height: 100%;
        }

        /* Mỗi phiếu GGH */
        .ggh-ticket {
            border: 1px solid #000;
            padding: 3mm;
            page-break-inside: avoid;
            break-inside: avoid;
            display: flex;
            flex-direction: column;
            position: relative;
        }

        .ggh-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 2mm;
        }

        .logo {
            flex-shrink: 0;
        }

        .logo img {
            width: 40px;
            height: auto;
        }

        .company-info {
            text-align: right;
            font-weight: bold;
            font-size: 9px;
            flex-grow: 1;
        }

        .ggh-title {
            text-align: center;
            font-weight: bold;
            font-size: 12px;
            margin: 1mm 0;
            text-transform: uppercase;
        }

        .order-code {
            text-align: center;
            font-size: 9px;
            margin-bottom: 2mm;
        }

        .red {
            color: red;
        }

        .ggh-content {
            flex-grow: 1;
            font-size: 9px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .ggh-content p {
            margin: 1mm 0;
            line-height: 1.2;
        }

        /* Thiết lập in ấn */
        @media print {
            @page {
                size: A4;
                margin: 0;
            }

            body {
                margin: 0;
                padding: 5mm;
                width: 210mm;
                height: 297mm;
            }

            .container {
                gap: 5mm;
            }

            .ggh-ticket {
                border: 1px solid #000;
                padding: 3mm;
            }
        }

        /* Ẩn tiêu đề trình duyệt khi in */
        @media print {
            .no-print {
                display: none;
            }
        }
    </style>

    <% if (typeof autoPrint !=='undefined' && autoPrint) { %>
        <script>
            window.onload = function () {
                setTimeout(function () {
                    window.print();
                    setTimeout(function () {
                        if (window.opener) {
                            window.close();
                        }
                    }, 500);
                }, 1000);
            };
        </script>
        <% } %>
</head>

<body>
    <h1 class="no-print" style="text-align: center; margin-bottom: 10px;">Giấy Gửi Hàng - Xem trước khi in</h1>

    <div class="container">
        <% for(let i=1; i <=6; i++) { %>
            <div class="ggh-ticket">
                <div class="ggh-header">
                    <div class="logo">
                        <img src="<%= logoBase64 %>" alt="Logo">
                    </div>
                    <div class="company-info">
                        CÔNG TY CỔ PHẦN CÔNG NGHIỆP M.E.C.I
                    </div>
                </div>

                <h2 class="ggh-title">GIẤY GỬI HÀNG</h2>

                <p class="order-code">(MÃ ĐƠN HÀNG: <span class="red">
                        <%= donHang[6] %>
                    </span>)</p>

                <div class="ggh-content">
                    <p>- Người nhận hàng: <span class="red">
                            <%= donHang[17] || donHang[20] || '' %>
                        </span></p>
                    <p>- Điện thoại: <span class="red">
                            <%= donHang[25] || donHang[27] || donHang[29] || '' %>
                        </span></p>
                    <p>- Địa chỉ: <span class="red">
                            <%= donHang[24] || donHang[26] || donHang[28] || '' %>
                        </span></p>
                    <p>- Người gửi hàng: <span class="red">
                            <%= donHang[2] %>
                        </span></p>
                    <p>- Điện thoại: <span class="red">
                            <%= donHang[52] %>
                        </span></p>
                    <p>- Địa điểm sản xuất, kinh doanh: Số 162, đường 916, thôn La Gián, xã Đoài Phương, thành phố Hà
                        Nội</p>
                </div>
            </div>
            <% } %>
    </div>
</body>

</html>