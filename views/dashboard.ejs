<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Dashboard Kinh Doanh</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        h1 { text-align: center; }
        table { border-collapse: collapse; width: 100%; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: center; }
        th { background-color: #f4f4f4; }
    </style>
</head>
<body>
    <h1>📊 Dashboard Kinh Doanh</h1>

    <h2>📈 Tỷ lệ chốt đơn: <%= conversionRate %>%</h2>

    <h2>Doanh thu theo tháng</h2>
    <table>
        <tr><th>Tháng</th><th>Doanh thu</th><th>Lợi nhuận</th></tr>
        <% Object.keys(revenue).forEach(thang => { %>
            <tr>
                <td><%= thang %></td>
                <td><%= revenue[thang].toLocaleString() %></td>
                <td><%= profit[thang].toLocaleString() %></td>
            </tr>
        <% }) %>
    </table>

    <h2>🏆 Top 10 sản phẩm bán chạy</h2>
    <table>
        <tr><th>Mã SP</th><th>Số lượng</th></tr>
        <% topProducts.forEach(p => { %>
            <tr>
                <td><%= p.maSP %></td>
                <td><%= p.sum %></td>
            </tr>
        <% }) %>
    </table>
</body>
</html>
